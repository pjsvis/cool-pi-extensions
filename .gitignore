# Flox — deprecated (see decisions/008-deprecate-flox.md). Ignore all artifacts.
.flox/

# Runtime data (moved to data/ folder - tracked, visible)
# .silo/ deleted - data is now in data/ directory

# Node
cli/pi-models/node_modules/
cli/pi-check/node_modules/

# macOS
.DS_Store

# Binary PDF clippings (kept local; .md is the repo's clipping convention)
*.pdf
.todos/
.sidecar/
.sidecar-agent
.sidecar-task
.sidecar-pr
.sidecar-start.sh
.sidecar-base
.td-root
# Skate setup scripts (contain secrets)
skate-omarchy-setup.sh
skate-*-setup.sh

# Tool-generated indexes (not source — rebuilt locally)
.codegraph/
.understand-anything/
